[profile.default]
src = 'src'
out = 'out'
libs = ['lib']

# See more config options https://github.com/foundry-rs/foundry/tree/master/config
[rpc_endpoints]
testbsc = "${TESTBSC_RPC_URL}"

[etherscan]
testbsc = { key = "${ETHERSCAN_API_KEY}" }

