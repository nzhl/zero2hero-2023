# zero2hero-2023


## Get Started

1. update `.env`

2. useful commands:
```bash
forge test -w -vv  # dev
forge build # build
forge script Deploy --rpc-url testbsc --broadcast --verify -vvvv # deploy & verify
```